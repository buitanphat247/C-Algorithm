#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    long long n,m,s,kq=0;
    cin>>n>>m>>s;
    long long x[n],z[m];
    for(long long i=1; i<=n; i++)
        cin>>x[i];
    for(long long i=1; i<=m; i++)
        cin>>z[i];
    long long i=1,j=m;
    while(i<=n&&j>=1)
    {

    }
}
